apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-generator" (include "common.names.fullname" .) }}
rules:
  - apiGroups:
      - ''
    resources:
      - "secrets"
      - "configmaps"
    verbs:
      - "get"
      - "patch"
      - "update"
